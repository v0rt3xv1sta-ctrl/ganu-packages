# Ganu packages
ganu packages is all the packages for the new pkg package manager in my linux distro
# How to upload
to upload simply add the tar.gz file and use the package name of your package before .tar.gz then to make it work just update index with the package on a new line
